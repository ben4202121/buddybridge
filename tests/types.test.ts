import { DEFAULT_SETTINGS, migrateSettings } from '../src/types';
describe('DEFAULT_SETTINGS', () => { it('should accept empty codebuddyPath', () => { expect(DEFAULT_SETTINGS.codebuddyPath).toBe(''); }); it('should have sensible maxConversations', () => { expect(DEFAULT_SETTINGS.maxConversations).toBeGreaterThan(0); }); });
describe('migrateSettings', () => { it('should return defaults for null', () => { const r = migrateSettings(null); expect(r.codebuddyPath).toBe(''); }); it('should merge stored values', () => { const r = migrateSettings({ codebuddyPath: '/custom/codebuddy', maxConversations: 10 }); expect(r.codebuddyPath).toBe('/custom/codebuddy'); expect(r.maxConversations).toBe(10); }); });